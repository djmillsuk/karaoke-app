services:
  karaoke:
    build: .
    container_name: karaoke-songbook
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      CSV_PATH: /data/songs.csv
    volumes:
      - ./data:/data:ro
